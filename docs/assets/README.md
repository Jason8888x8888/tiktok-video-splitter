# Example asset provenance

`output-directory-example.png` is a repository-maintainer-provided screenshot of a real output folder layout. It contains filenames and thumbnails only; it does not include source media, account identifiers, credentials, or local filesystem paths.

The screenshot is included as project documentation under the repository license.
